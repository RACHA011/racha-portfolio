{
  "name": "portfolio",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm start --prefix client\" \"node server.js --prefix server\"",
    "start-client": "cd client && npm start",
    "start-server": "cd server && node server.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "fs": "^0.0.1-security",
    "portfolio": "file:"
  }
}
